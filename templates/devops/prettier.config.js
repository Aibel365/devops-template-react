//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    trailingComma: "none",
    tabWidth: 4,
    semi: true,
    singleQuote: false,
    printWidth: 256,
    proseWrap: "always",
    endOfLine: "lf",
    singleAttributePerLine: true
};

export default config;
